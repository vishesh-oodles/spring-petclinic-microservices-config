# ﻿server:
#   port: 8761
#   servlet:
#     context-path: /eureka

# eureka:
  # client:
  #   registerWithEureka: false
  #   fetchRegistry: false
  # server:
  #   maxThreadsForPeerReplication: 0

server:
  port: 8761
  servlet:
    context-path: /eureka-service

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    maxThreadsForPeerReplication: 0
  dashboard:
    path: /eureka
    resourcePath: classpath:/static/eureka/
