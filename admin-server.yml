server:
  port: 9091

---
spring:
  config:
    activate:
      on-profile: docker

  application:
    name: admin-server

  boot:
    admin:
      context-path: /
      adminServer:
        enabled: true
      client:
        url: http://admin-server:9091

  cloud:
    config:
      enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/
